var routes = [
	{
    	desc: 'callback route',
    	method: 'post',
        route: '/callback',
        protected: false,
        callback: function(req, res){
		}
    }
];


module.exports.routes = routes;
